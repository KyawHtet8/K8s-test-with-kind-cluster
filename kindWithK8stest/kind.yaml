kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30001  # Port inside the container/node
        hostPort: 30001       # Port on your local machine (localhost)
        protocol: TCP
  - role: worker
  - role: worker
